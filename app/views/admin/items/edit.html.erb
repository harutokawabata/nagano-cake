<div class="container">
  <div class="row">
    <h2><span>商品編集</span></h2>
  </div>
  <div class="row">
   <%= form_with url:admin_item_path(@item), method: :patch, model:@item, local:true do |f| %>
    <div class="col-xs-4">
      <p>写真表示</p>
      <%= attachment_image_tag @item, :image, format: 'jpeg', size: "100x100" %><br>
      <%= f.attachment_field :image %>
    </div>
    <div class="col-xs-4">
        <label>商品名</label>
        <%= f.text_field :name, autofocus: true %><br>
        <label>商品説明</label>
        <%= f.text_area :introduction, autofocus: true %><br>
        <label>ジャンル</label>
        <%= f.collection_select :genre_id, Genre.all, :id, :name, include_blank: "選択して下さい" %><br>
        <%# f.collection_select :保存先のカラム名(itemモデル）, これ以降genreモデルのデータ→テーブル（モデル）名.all, :実際にDBに保存させるカラム名(id), :選択肢に用いるカラム名(name), include_blank: "選択して下さい" %>
        <label>税抜価格</label>
        <%= f.text_field :price, autofocus: true %><br>
        <label>販売ステータス</label>
        <%= f.select :is_active, [["有効", "true"], ["無効", "false"]], include_blank: "選択してください" %>
        <%= f.submit "変更を保存" %>
    </div>
   <% end %>
  </div>
</div>